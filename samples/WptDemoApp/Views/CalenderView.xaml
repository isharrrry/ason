<UserControl x:Class="WpfSampleApp.Views.CalenderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:WpfSampleApp.Views"
             xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        <local:DateOnlyConverter x:Key="DateOnlyConverter" />
        <CollectionViewSource x:Key="GroupedAppointments"
                              Source="{Binding Appointments}">
            <CollectionViewSource.GroupDescriptions>
                <PropertyGroupDescription PropertyName="StartTime"
                                          Converter="{StaticResource DateOnlyConverter}" />
            </CollectionViewSource.GroupDescriptions>
            <CollectionViewSource.SortDescriptions>
                <scm:SortDescription PropertyName="StartTime"
                                     Direction="Ascending" />
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>
        <DataTemplate x:Key="appointmentTemplate">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <TextBlock Text="{Binding Title}"
                           TextTrimming="WordEllipsis"
                           FontWeight="SemiBold" />
                <TextBlock Text="{Binding Description}"
                           TextTrimming="WordEllipsis"
                           Grid.Row="1" />
            </Grid>
        </DataTemplate>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadDataCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <ListBox ItemsSource="{Binding Source={StaticResource GroupedAppointments}}"
                 AutomationProperties.AutomationId="CalendarAppointments"
                 ItemTemplate="{StaticResource appointmentTemplate}"
                 Background="White"
                 Padding="8">
            <ListBox.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name, StringFormat='{}{0:dddd, MMMM d}'}"
                                       FontWeight="Bold"
                                       Margin="0,5,0,2" />
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListBox.GroupStyle>
        </ListBox>
        <ProgressBar IsIndeterminate="True"
                     VerticalAlignment="Center"
                     Width="200"
                     Visibility="{Binding LoadDataCommand.IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}" />
    </Grid>
</UserControl>
