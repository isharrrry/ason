<UserControl x:Class="WpfSampleApp.Views.ChartsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfSampleApp.Views"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Loaded="OnLoaded"
             Unloaded="OnUnloaded">
    <Grid Background="White">
        <ContentControl x:Name="chartArea"
                        Margin="20">
            <TextBlock Text="Ask the agent to generate a chart from the app’s data"
                       AutomationProperties.AutomationId="ChartsViewHint"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       FontWeight="SemiBold" />
        </ContentControl>

    </Grid>
</UserControl>
