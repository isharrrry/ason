<Window x:Class="WpfSampleApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WpfSampleApp.ViewModels"
        xmlns:v="clr-namespace:WpfSampleApp.Views"
        xmlns:local="clr-namespace:WpfSampleApp"
        Icon="/Images/appIcon.png"
        mc:Ignorable="d"
        Title="MainWindow" Height="900" Width="1400"
        Background="#F3F3F3">
    <!-- The background is set explicitly so all three target frameworks agree: on net9/net10 the platform
         Fluent theme would supply it, on net6 the fallback theme does not style the window itself. -->
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:EmployeesViewModel}">
            <v:EmployeesView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:EmailsViewModel}">
            <v:EmailsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CalendarViewModel}">
            <v:CalenderView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ChatViewModel}">
            <v:ChatView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ChartsViewModel}">
            <v:ChartsView />
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="10" /> 
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="10" /> 
            <ColumnDefinition Width="385" />
        </Grid.ColumnDefinitions>
        <ListBox ItemsSource="{Binding NavigatoinItems}"
                 AutomationProperties.AutomationId="NavigationList"
                 SelectedItem="{Binding CurrentNavigationItem}"
                 Background="Transparent"
                 DisplayMemberPath="Name">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <!-- DisplayMemberPath only affects rendering: without this the UIA name of each
                         item stays the CLR type name, which breaks screen readers and automation. -->
                    <Setter Property="AutomationProperties.Name" Value="{Binding Name}" />
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
        <GridSplitter Grid.Column="1"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      ResizeBehavior="PreviousAndNext"
                      ResizeDirection="Columns"
                      Background="Transparent"
                      ShowsPreview="False"
                      Focusable="False"
                      Width="10" />
        <ContentControl Content="{Binding CurrentNavigationItem.ViewModel}" 
                        Margin="0,0,0,8"
                        Grid.Column="2"/>
        <GridSplitter Grid.Column="3"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      ResizeBehavior="PreviousAndNext"
                      ResizeDirection="Columns"
                      Background="Transparent"
                      ShowsPreview="False"
                      Focusable="False"
                      Width="10" />
        <ContentControl Content="{Binding ChatViewModel}"
                        Grid.Column="4" />
    </Grid>
</Window>
